🎰 Go Slot Machine (Refactored)

Учебный проект - консольное приложение имитирующее игру в слот машине, является рефакторингом другого учебного проекта из видео на youtube, который представлял из себя монолитное приложение без соблюдения каких-либо принципов программирования, оригинальный код старого приложения можно посмотреть в ветке legacy-monolith на странице проекта в гитхаб: https://github.com/Moha2024/go-slot-machine

Весь рефакторинг приложения и составление юнит тестов был произведён мной, ИИ использовался только для консультаций и составления таблиц с данными для юнит тестов.

Я решил сделать этот проект для получения практического опыта после прочтения книги Head First GO за авторством Джея Макгаврена.

Информация ниже была составлена ИИ после того как я попросил его сравнить проекты.

Это учебный проект, который прошел путь от одного файла с перемешанной логикой до расширяемого приложения. В игре реализованы классические механики: ставки, случайная генерация символов, проверка выигрышных линий и управление балансом игрока.
Основные возможности:

    Динамическая сетка: Настройка любого количества строк и столбцов.

    Система вероятностей: Распределение символов на барабанах через конфигурацию.

    Командный интерфейс: Поддержка игровых сессий (Restart, Quit).

    Unit-тестирование: Покрытие бизнес-логики тестами более чем на 80%.

🏗 Эволюция: От Монолита к Архитектуре

Проект был полностью переработан. Ниже приведено сравнение подходов:
Характеристика	    Старый Монолит (legacy/)	                Новая Версия (internal/)
Структура	        Один файл main.go           	            Разделение на domain, service, ui, pkg
Генерация чисел	    Прямая зависимость от math/rand	            Инверсия зависимостей (Interface NumberGenerator)
Тестируемость	    Невозможно тестировать части логики	        Полное покрытие Unit-тестами с использованием Mock-объектов
Гибкость	        Жестко прописанные параметры	            Конфигурация через internal/config
Ввод/Вывод	        Перемешан с расчетами	                    Изолирован в пакете ui
Ключевой рефакторинг:

Одним из главных достижений стал уход от глобального состояния и прямого использования рандома. Теперь генератор чисел внедряется как зависимость:

// Это позволяет легко подменить реальный рандом на Mock в тестах
type NumberGenerator interface {
    NumberGenerator(min int, max int) int
}

📂 Структура проекта

.
├── internal/
│   ├── config/          # Параметры игры (символы, множители, баланс)
│   ├── domain/
│   │   └── models/      # Сущности (Player, SlotMachine) и логика их работы
│   ├── service/         # Бизнес-логика (ExecuteRound, валидация ставок)
│   └── pkg/
│       └── rng/         # Реализация генератора случайных чисел
├── ui/                  # Консольный интерфейс и управление циклом игры
└── main.go              # Точка входа и инициализация зависимостей

🛠 Запуск и тестирование
Требования:

    Go 1.21 или выше

Запуск игры:

go run main.go

Запуск тестов:

Проект включает в себя детальные тесты для проверки механики выигрышей, валидации ставок и работы с балансом.
Bash

go test ./... -v

📝 Извлеченные уроки

В процессе рефакторинга были решены следующие задачи:

    Устранение дублирования: Логика проверки линий была отделена от логики отображения.

    Безопасность типов: Переход на использование uint для баланса и ставок, исключающий отрицательные значения на уровне типов.

    Обработка ошибок: Валидация ввода пользователя теперь происходит в сервисном слое, а не в основной петле игры.

    Mocking: Написание собственного mockGenerator для имитации определенных игровых ситуаций (например, выпадение джекпота) в тестах.
